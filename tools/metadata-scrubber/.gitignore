node_modules/
dist/
.DS_Store
*.log

# Incremental TypeScript build state. Machine-specific, changes on every build,
# and was being tracked — every `npm run build` produced a spurious diff.
*.tsbuildinfo

# Per-machine permission grants. launch.json IS tracked (it starts the dev
# server and is useful to anyone cloning); this one is not.
.claude/settings.local.json
.env
.env.local
coverage/
.superpowers/

# Real fixtures the engineer must place locally
tests/fixtures/iphone.heic
